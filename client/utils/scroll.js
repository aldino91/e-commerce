export function scrollY1(setShowCard1) {
  if (window.scrollY >= 400) {
    // console.log("position", scrollY);
    setShowCard1(true);
  }
}

export function scrollY2(setShowCard2) {
  if (window.scrollY >= 800) {
    setShowCard2(true);
  }
}

export function scrollY3(setShowCard3) {
  if (window.scrollY >= 1110) {
    setShowCard3(true);
  }
}
export function scrollY4(setShowCard4) {
  if (window.scrollY >= 1400) {
    setShowCard4(true);
  }
}
export function scrollY5(setShowCard5) {
  if (window.scrollY >= 1600) {
    setShowCard5(true);
  }
}
export function scrollY6(setShowCard6) {
  if (window.scrollY >= 1950) {
    setShowCard6(true);
  }
}

export function scrollY7(setShowCard7) {
  if (window.scrollY >= 2240) {
    setShowCard7(true);
  }
}
